# Window C — Rating Ledger UI — Session Log

Last updated: 2026-04-21
Branch: `session/ledger-2026-04-21`
Worktree: `.worktrees/wt-ledger`
Brief: `docs/briefs/2026-04-21-window-C-rating-ledger.md`

Deliverable: `/dashboard/rating-ledger` — a unified, read-only view of every rating Oliver has given across all three surfaces (legacy Prompt Lab, Phase 2.8 Listings Lab, production scene_ratings), each row linked back to the source image, rendered clip, SKU, stars, reasons, comment, and retrieval-readiness.

## Schema mapping (confirmed during kickoff read)

| surface | table | image | clip | SKU | reasons | `has_embedding` | `has_model_used` | recipe link |
|---|---|---|---|---|---|---|---|---|
| `legacy_lab` | `prompt_lab_iterations` | `prompt_lab_sessions.image_url` (join on `session_id`) | `clip_url` | `provider` (no SKU) | n/a | `embedding IS NOT NULL` | `false` | `prompt_lab_recipes.source_iteration_id = id` |
| `listings_lab` | `prompt_lab_listing_scene_iterations` | `prompt_lab_listing_photos.image_url` (join on scene.photo_id) | `clip_url` | `model_used` (native) | `rating_reasons TEXT[]` | `embedding IS NOT NULL` | `true` | same |
| `prod` | `scene_ratings` | `photos.file_url` (join on scene.photo_id) | `rated_clip_url` → fallback `scenes.clip_url` | `rated_provider` → fallback `scenes.provider` (provider family only) | n/a | `rated_embedding OR scenes.embedding` | `false` | n/a |

## Self-check 1 — kickoff (pre-code)

- **(a) North-Star criterion served** — #1 "no HITL" via transparency. Every rating Oliver sees now has a visible trail into retrieval; ratings stop "going into an abyss." Secondary: seeds the audit surface for #2 "no hallucinations" — mismatches between star + rendered clip + source photo become findable.
- **(b) Highest-leverage next step?** Yes. API endpoint first (UI blocked on it). Pivot trigger = "unifying all three surfaces blocks >1h" — in that case ship the Phase 2.8 listings_lab surface alone first (largest active signal + cleanest native columns) and iterate.
- **(c) Evidence it's working?** Pre-code. Required-reading done (HANDOFF, PROJECT-STATE, daily-engagement spec, ML audit 2026-04-20, README). Confirmed: migration 014 denormalizes prod scene_ratings so rated context survives re-runs; migration 023 defines listings-lab tables; migration 026 adds `rating_reasons TEXT[]`; migration 028 adds `model_used` on recipes + maps legacy provider→'kling-v2-native'.
- **(d) Pivot?** No.

## Plan

1. `api/admin/rating-ledger.ts` — GET, `requireAdmin`. Fetches all three surfaces in parallel, maps to unified `LedgerRow[]`, applies filter + sort + paginate. Return `{ rows, total, counts: { legacy_lab, listings_lab, prod } }`.
2. `src/pages/dashboard/RatingLedger.tsx` — table page. Toolbar: surface chip row + SKU combobox + min-rating select + has-comment toggle. Columns: image thumb · clip preview · SKU · stars · reasons · comment · surface · retrieval chip. Default sort `rated_at DESC`.
3. Wire route `/dashboard/rating-ledger` in `src/App.tsx`. Add nav link in `TopNav.tsx` Development dropdown (matches convention for admin dev tooling).
4. Smoke test via `npm run dev`. Confirm ≥3 rows across ≥2 surfaces, thumbs + clip load, at least one filter works.
5. Commit in three chunks — (i) endpoint, (ii) page + route, (iii) nav + docs via docs-subagent.

## Budget

Zero renders. This is a read-only UI. Any accidental touch of a render path is a STOP signal.

## Self-check 2 — post-build (pre-commit of page + route)

- **(a) Criterion served** — #1 "no HITL" via transparency. Retrieval-status chip is the core of the audit surface for #2 "no hallucinations" (ratings missing embedding or SKU flagged amber/red so Oliver can see coverage holes).
- **(b) Highest-leverage next step?** Commit chunk 2 (page + route + nav + client helper), then ship docs + memory via subagent. No pivot.
- **(c) Evidence it's working?**
  - `vite build` passes cleanly (2.95s, no type errors in the new files).
  - `npx tsc --noEmit -p tsconfig.api.json` — zero errors on `rating-ledger.ts` (pre-existing errors in poll-lab-renders.ts unrelated).
  - `npx tsc --noEmit -p tsconfig.app.json` — zero errors on the new files (pre-existing errors in `PromptLab.tsx` + `labNextAction.test.ts` unrelated).
  - `npx eslint` — clean on all four new/edited files.
  - `npx vitest run` — 67/67 tests pass, no regressions.
  - `npm run dev` starts Vite on port 8080. `curl http://localhost:8080/dashboard/rating-ledger` returns 200 (SPA shell). `curl http://localhost:8080/src/pages/dashboard/RatingLedger.tsx` returns the transformed module — Vite compiles the page without errors.
- **(d) Pivot?** No.

## Smoke-test limitations (explicitly called out)

`npm run dev` runs Vite only. The `/api/admin/rating-ledger` serverless function is **not** served by Vite (there's no proxy in `vite.config.ts`). Full end-to-end verification (≥3 rows rendering across ≥2 surfaces, image + clip loading) requires either:
1. Running `vercel dev` (worktree is not linked to Vercel project; don't link from a non-main worktree) OR
2. Oliver loading the deployed preview URL in a browser with an active admin Supabase session.

What I verified autonomously:
- The page compiles and mounts (Vite 200 on `/dashboard/rating-ledger`).
- The API endpoint loads in `tsc` without any schema mismatch against `lib/client.ts` + `lib/auth.ts`.
- Query logic mirrors the real schemas confirmed via migrations 002/014/023/026/028.

What needs a browser session:
- Confirming ≥3 rows actually render.
- Confirming thumb + clip URLs are reachable.
- Confirming at least one filter narrows the set.

Flagging this so the coordinator can either pick it up with a logged-in session or post a short Loom.

