{
  "name": "reelready",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered real estate video automation pipeline",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "worker": "tsx worker/index.ts",
    "worker:dev": "tsx watch worker/index.ts",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "workspaces": [
    "packages/*",
    "worker"
  ],
  "packageManager": "npm@10.8.0",
  "engines": {
    "node": ">=20"
  }
}
